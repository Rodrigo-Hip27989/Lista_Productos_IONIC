import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.cap.toronja.mis_gastos',
  appName: 'Mis Gastos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
